<h1>New Image</h1>

<%= simple_form_for @image do |f| %>
  <%= f.input :url, label: 'Url: ' %>
  <%= f.input :tag_list, label: 'Tags: ' %>
  <%= f.button :submit %>
<% end %>
<%= link_to 'Back', images_path %>
